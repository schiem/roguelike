#include <stdio.h>
#include <iostream>
#include <vector>
#include "Display.h"
using namespace std;

int main(int argc, char* args[])
{
    Display d;
    /*
    d.refresh();
	bool running = true;
	while( running )
    {
		running = d.get_input();
		//act();
		//display();
        d.refresh();
	}
	d.cleanup();
	return 0;	
    */
}
